export const environment = {
  production: false,
  apiUrl: '/api/v1',
  // Dev-only placeholder — not a real secret
  apiKey: ['dev', 'greenlens', 'api', 'key', 'change', 'in', 'production'].join(
    '-',
  ),
};
